# Orchestra runtime state — never commit these
*.env
*.log
PLAN.md
TASKS.json
review-comments.md
brain-state.md
auto-progress.md
auto-halt-*.md
auto-summary-*.md
halts/
logs/

# Claude Code project-level directory — entirely runtime; never commit
# (agents and commands live in top-level agents/ and commands/ instead)
.claude/

# Editor / OS
.DS_Store
*~
*.swp
